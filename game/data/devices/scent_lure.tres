[gd_resource type="Resource" script_class="DeviceKind" load_steps=2 format=3]

[ext_resource type="Script" path="res://game/data/device_kind.gd" id="1_device"]

[resource]
script = ExtResource("1_device")
id = "scent_lure"
display_name = "Scent Lure"
description = "Reeks of something worth eating. Pulls wanderers in from further than any funnel, and it does not need a web around it — drop one where you want traffic and traffic appears."
effect = 1
colour = Color(0.95, 0.75, 0.35, 1)
radius = 14.0
one_shot = false
stack_limit = 4
reports = true
listens = false
